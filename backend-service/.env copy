# 应用配置
APP_NAME=AI医疗助手后端服务
APP_VERSION=1.0.0
DEBUG=True
HOST=0.0.0.0
PORT=8010

# 数据库配置
# 注意: Docker部署时使用5433端口(避免与现有PostgreSQL冲突)
DATABASE_URL=postgresql://medical_user:medical_pass@localhost:5433/medical_ai
DATABASE_ECHO=False

# Redis配置
# 注意: Docker部署时使用6380端口(避免与现有Redis冲突)
REDIS_URL=redis://localhost:6380/0

# Deepseek AI配置(云端API)
DEEPSEEK_API_KEY=sk-4574b9e7f724460e88eaba27b87bd3e7
DEEPSEEK_API_BASE=https://api.deepseek.com/v1
DEEPSEEK_MODEL=deepseek-chat

# 本地AI配置(Ollama)
AI_SERVICE_MODE=local  # local | cloud | hybrid
LOCAL_AI_ENDPOINT=http://localhost:11434
LOCAL_AI_MODEL=qwen2.5vl:latest
LOCAL_AI_TIMEOUT=60
LOCAL_AI_MAX_RETRIES=3

# JWT配置
SECRET_KEY=your-secret-key-here-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS配置
CORS_ORIGINS=["http://localhost:9527", "http://localhost:3010"]

# 日志配置
LOG_LEVEL=INFO
LOG_FILE=logs/app.log

